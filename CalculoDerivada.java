//CALCULO DERIVADA APO2
public class CalculoDerivada {
    public static double calcularDerivada(double x, double h) {
        double f_x = f(x);  // Valor de la función en x
        double f_x_h = f(x + h);  // Valor de la función en x + h
        return (f_x_h - f_x) / h;  // Aproximación de la derivada
    }

    public static double f(double x) {
        // Define tu función aquí. Por ejemplo, f(x) = x^2.
        return x * x;
    }

    public static void main(String[] args) {
        double x = 2.0;  // Punto en el que se calcula la derivada
        double h = 0.0001;  // Tamaño del paso
        double derivada = calcularDerivada(x, h);
        System.out.println("La derivada en x=" + x + " es aproximadamente " + derivada);

        import org.jfree.chart.ChartFactory;
import org.jfree.chart.ChartPanel;
import org.jfree.chart.JFreeChart;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart

    }
}